import { UtilisateurConnect } from './utilisateur-connect';

describe('UtilisateurConnect', () => {
  it('should create an instance', () => {
    expect(new UtilisateurConnect()).toBeTruthy();
  });
});
